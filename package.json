{
  "name": "dataloader-group-batch-load-fn",
  "scripts": {
    "test": "vitest --coverage",
    "generate-types": "npx -p typescript tsc src/**/*.js --declaration --allowJs --emitDeclarationOnly --outDir types"
  },
  "types": "./types",
  "version": "1.0.0",
  "main": "./src/index.js",
  "license": "MIT",
  "dependencies": {
    "object-hash": "^3.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.6",
    "dataloader": "^2.2.2",
    "vitest": "^0.34.6"
  }
}
